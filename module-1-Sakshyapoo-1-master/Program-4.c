//INPUT: 25 4
//OUTPUT: Quotient = 6 and Remainder = 1
#include <stdio.h>
int main() {
 int dividend, divisor, quotient, remainder;
 scanf("%d%d", &dividend,&divisor); //correct the code

 // Computes quotient
 quotient = dividend / divisor; //corect the code
 // Computes remainder
 remainder = dividend % divisor; // correct the code
 printf(" Quotient = %d Remainder = %d", quotient,remainder); //correct code
 return 0;
}